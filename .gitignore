# 你自己在本机产生的内容，不要提交到仓库里。
#
# 为什么要有这个文件：
#   你真的可能手滑 `git add -A`。下面这些目录里是你的提问、AI 给你的回答、
#   你交的作业 —— 它们属于你个人，没有任何理由进公开仓。
#
# ⚠️ 改动过这个文件的话请往下读一遍再走：**忽略规则必须按父目录写**。
#    踩过的坑（端侧实测，学生机器上真实复现）：
#      旧规则忽略的是 课程问题池/问题条目/ 与 课程问题池/FAQ/ —— 那是**旧布局**。
#      而面板实际把提问写到 课程问题池/学生/<你的学号>/、把公开答疑同步到 课程问题池/公共/。
#      于是被忽略的三个目录一个都没盖住真正的写入位置，一次 `git add -A`
#      就会把自己的全部提问连同 AI 答复推到公开仓。
#    所以这里一律忽略**父目录**，而不是枚举子目录 —— 以后面板换目录名也盖得住。

# 你的提问、AI 给你的回答、以及从公共池同步下来的内容
# （整个 课程问题池/ 都是本机数据，没有一样该进公开仓）
课程问题池/

# 你交的作业（含 AI 按教案给出的初筛意见）
作业提交/

# 本机身份（学号与姓名）：只在本机，绝不进任何仓库
我的身份.json

# 编辑器与系统杂项
.vscode/
.idea/
Thumbs.db
desktop.ini
*.tmp
*.bak
*.orig

# 面板运行缓存（可随时重建）
课程中心/.cache/

# 注意：课程中心/_插件源码/ **不能忽略**。
#   那份 panel.css 是面板优先加载的样式表（工作区副本优先于插件内置那份），
#   它必须随仓库分发，否则学生机器上会退回插件里一份陈旧的 lib/panel.css ——
#   端侧实测过：69 个 class 因此没有样式，鱼骨图、课时脉络、提问列表全部塌掉。
